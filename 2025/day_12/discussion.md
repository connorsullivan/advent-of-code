# Day 12

## Part One
Treat each shape’s area as the limiting factor. Parse the shape definitions to compute their occupied cell counts, then for each region line compute the total required area (count × shape area). If that total doesn’t exceed the region’s area (width × height), the presents can fit; count such regions.

Model used: GPT-4o-mini.
